#!/bin/zsh
set -euo pipefail

export PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:$PATH"

WAKATIME_BIN="${HOME}/.wakatime/wakatime-cli"

if [[ ! -x "${WAKATIME_BIN}" ]]; then
  echo "WakaTime CLI not found at ${WAKATIME_BIN}" >&2
  exit 1
fi

exec "${WAKATIME_BIN}" "$@"
