#!/bin/zsh
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "Usage: jira-search 'project = TEAM ORDER BY updated DESC' [additional acli search args]" >&2
  exit 2
fi

jql="$1"
shift

exec "${HOME}/bin/acli-codex" jira workitem search --jql "${jql}" "$@"
